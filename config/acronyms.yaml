# Acronym normalization rules for F5 Distributed Cloud API specifications
# Format: lowercase_term: NORMALIZED_FORM
# Synchronized with f5xc-api-mcp project (142+ acronyms)

acronyms:
  # F5 and Product Specific
  f5: F5
  xc: XC
  volterra: Volterra
  ves: VES
  big-ip: BIG-IP
  bigip: BIG-IP
  nginx: NGINX
  adc: ADC

  # Protocols and Standards
  api: API
  apis: APIs
  rest: REST
  url: URL
  urls: URLs
  uri: URI
  uris: URIs
  http: HTTP
  https: HTTPS
  tcp: TCP
  udp: UDP
  ip: IP
  ipv4: IPv4
  ipv6: IPv6
  tls: TLS
  ssl: SSL
  mtls: mTLS
  dns: DNS
  fqdn: FQDN
  grpc: gRPC
  ssh: SSH
  smtp: SMTP
  imap: IMAP
  pop3: POP3
  ftp: FTP
  sftp: SFTP
  rdp: RDP
  snmp: SNMP
  ntp: NTP
  ldap: LDAP
  saml: SAML
  oidc: OIDC
  oauth: OAuth
  jwt: JWT
  sni: SNI
  rfc: RFC
  iso: ISO
  utc: UTC

  # Security
  waf: WAF
  ddos: DDoS
  xss: XSS
  csrf: CSRF
  sql: SQL
  sqli: SQLi
  owasp: OWASP
  cve: CVE
  rbac: RBAC
  acl: ACL
  mfa: MFA
  sso: SSO
  pki: PKI
  ca: CA
  crl: CRL
  ad: AD
  radius: RADIUS
  tacacs: TACACS
  ipsec: IPsec

  # Cloud and Infrastructure
  cdn: CDN
  vpc: VPC
  vnet: VNet
  aws: AWS
  gcp: GCP
  azure: Azure
  k8s: K8s
  kubernetes: Kubernetes
  vm: VM
  vms: VMs
  ce: CE
  re: RE
  pop: POP
  ec2: EC2
  eks: EKS
  aks: AKS
  gke: GKE
  saas: SaaS
  paas: PaaS
  iaas: IaaS
  sdk: SDK
  crd: CRD
  crds: CRDs

  # Networking
  lb: LB
  nlb: NLB
  alb: ALB
  elb: ELB
  slb: SLB
  glb: GLB
  gslb: GSLB
  bgp: BGP
  ospf: OSPF
  asn: ASN
  cidr: CIDR
  nat: NAT
  snat: SNAT
  dnat: DNAT
  vip: VIP
  vpn: VPN
  nic: NIC
  mac: MAC
  arp: ARP
  mtu: MTU
  qos: QoS
  vlan: VLAN
  mpls: MPLS
  gre: GRE
  vxlan: VXLAN

  # Data and Formats
  json: JSON
  yaml: YAML
  xml: XML
  csv: CSV
  html: HTML
  css: CSS
  uuid: UUID
  id: ID
  ids: IDs

  # Technical Terms
  cpu: CPU
  gpu: GPU
  ram: RAM
  io: I/O
  iops: IOPS
  ssd: SSD
  hdd: HDD
  os: OS
  cli: CLI
  gui: GUI
  ui: UI
  ux: UX

  # Time and Metrics
  ttl: TTL
  rtt: RTT
  sla: SLA
  kpi: KPI

  # HTTP Methods (preserve case in context)
  get: GET
  post: POST
  put: PUT
  patch: PATCH
  delete: DELETE
  head: HEAD
  options: OPTIONS

# Words that should NOT be treated as acronyms even if they match patterns
exceptions:
  - is
  - as
  - at
  - in
  - on
  - to
  - or
  - an
  - be
  - by
  - do
  - go
  - if
  - it
  - me
  - my
  - no
  - of
  - ok
  - so
  - up
  - us
  - we
